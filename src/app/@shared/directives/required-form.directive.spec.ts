import { RequiredFormDirective } from './required-form.directive';

describe('RequiredFormDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredFormDirective();
    expect(directive).toBeTruthy();
  });
});
